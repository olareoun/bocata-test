<h1>Listing sales_headers</h1>

<table>
  <tr>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sales_headers.each do |sales_header| %>
  <tr>
    <td><%= sales_header.description %></td>
    <td><%= link_to 'Show', sales_header %></td>
    <td><%= link_to 'Edit', edit_sales_header_path(sales_header) %></td>
    <td><%= link_to 'Destroy', sales_header, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sales header', new_sales_header_path %>
